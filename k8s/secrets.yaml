# ====================================================================================

# FILE: k8s/secrets.yaml

# DESCRIPTION: Stores sensitive database credentials

# NOTE: In production, use external secret managers or sealed secrets

# ====================================================================================

apiVersion: v1
kind: Secret
metadata:
name: postgres-secret
namespace: secure-notes
type: Opaque
stringData:
  POSTGRES_USER: notesuser
  POSTGRES_PASSWORD: changeme123!
  POSTGRES_DB: notesdb
